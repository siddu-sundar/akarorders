<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.akarcontrols.akarorders">
    <uses-permission android:name="android.permission.SEND_SMS" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Akarorders">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".addorders"></activity>
        <activity android:name=".addservice"></activity>
        <activity android:name=".service"></activity>
        <activity android:name=".visitorcallregister"></activity>
        <activity android:name=".addnewvisitor"></activity>
        <activity android:name=".currentorders"></activity>
        <activity android:name=".enterserialnumber"
            android:windowSoftInputMode="adjustPan">
        </activity>
    </application>

</manifest>